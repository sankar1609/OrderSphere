apiVersion: v1
kind: Service
metadata:
  name: ordersphere-auth
  namespace: ordersphere
spec:
  type: ClusterIP
  selector:
    app: ordersphere-auth
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30227
