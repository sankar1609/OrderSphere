apiVersion: v1
kind: Service
metadata:
  name: ordersphere-gateway
  namespace: ordersphere
spec:
  type: NodePort
  selector:
    app: ordersphere-gateway
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30000
