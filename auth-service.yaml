apiVersion: v1
kind: Service
metadata:
  name: ordersphere-auth
  namespace: ordersphere
spec:
  selector:
    app: ordersphere-auth
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort

